# Informer model hyperparameters
# Note: d_model, n_heads, e_layers, d_ff, dropout are defined in hyperopt_config.yaml

model_params:
  d_layers:
    type: categorical
    choices: [1, 2]

  factor:
    type: categorical
    choices: [1, 3, 5]
